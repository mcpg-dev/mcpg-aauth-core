# mcpg-aauth-core

TODO: what this crate does and who should use it — written for an external
reader, not the team.

This repository is read-only: development happens upstream, and each release
is published here as a tagged snapshot. Issues are welcome. Consume the crate
by git reference:

```toml
[dependencies]
mcpg-aauth-core = { git = "https://github.com/mcpg-dev/mcpg-aauth-core", tag = "<release-tag>" }
```

## Building and testing

```sh
cargo build
cargo test
```
